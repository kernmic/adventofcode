const { {{toLowerCase componentName}} } = require("./{{componentFileName}}");
const { parseInput } = require("../0_util/input-parser");

const input = parseInput();

const sol1 = {{toLowerCase componentName}}(input);
